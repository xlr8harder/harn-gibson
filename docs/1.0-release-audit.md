# 1.0 Release Audit

Audit date: 2026-06-04

This audit maps the 1.0 feature set to current evidence. It is intentionally about release readiness, not the longer 1.1/1.2 visual roadmap.

## Verdict

Status: implementation-complete for the 1.0 substrate, pending final release verification.

The automated contract is strong: the local acceptance gate covers lint, full Python tests with 100% coverage, dynamic-port dogfood smoke, browser screenshot rendering, replay baselines, hard-coded renderer baselines, whitespace checks, and tracked secret/runtime hygiene. The remaining 1.0 work is release process and one real live dogfood pass with harn/Codex, not a known missing framework boundary.

## Evidence Commands

Recently verified locally:

- `uv run harn-gibson --help` lists the 1.0 command surface.
- `bash scripts/acceptance.sh --dry-run` lists the release gate commands.
- `bash scripts/acceptance.sh` passed on the latest non-docs release-candidate state, including:
  - `uv run ruff check .`
  - `uv run pytest` with 242 passed and 100% package coverage
  - dynamic-port dogfood smoke using `--harn-bin true`
  - generic replay screenshot/baseline suite
  - `gibson1` replay screenshot/baseline suite
  - `gibson1 --style mainframe` replay screenshot/baseline suite
  - dogfood stress-renderer replay screenshot/baseline suite
  - `git diff --check`
  - secret pattern scan
  - runtime config scan

The full acceptance gate should be rerun immediately before tagging 1.0 because replay baselines include repo line-count metadata.

## Requirement Audit

| Area | Status | Evidence | Release notes |
| --- | --- | --- | --- |
| Live event capture extension | Proven by tests | `tests/test_extension.py`, `tests/test_server_and_cli.py`, dogfood smoke in `scripts/acceptance.sh` | Needs one real harn/Codex dogfood pass before tag. |
| Browser input queue | Proven by tests | `tests/test_extension.py`, `tests/test_server_and_cli.py`, `tests/test_browser_integration.py` | Browser remains a lightweight input source, not harn session owner. |
| Persistent scene engine | Proven by tests | `tests/test_scene.py`, `tests/test_rendering.py`, browser integration tests | v1 mutation set is covered. |
| Tracebacks/runtime failures visible | Proven by tests | runtime diagnostic replay fixtures, `tests/test_extension.py`, `tests/test_server_and_cli.py` | Debug drawer is the expected surface. |
| World model | Proven by tests | `tests/test_world_model.py`, renderer context assertions in `tests/test_rendering.py` | Current model covers files, commands, changes, health, and outcomes with provenance. |
| Routing and coalescing | Proven by tests | `tests/test_routing.py`, `tests/test_rendering.py`, `tests/test_server_and_cli.py` | Stream buffering and route-rule sampling are in the contract. |
| Blocking and async modes | Proven by tests | render pipeline and CLI/env tests | Async remains fail-open and batch-oriented. |
| Renderer context and compaction | Proven by tests | `tests/test_rendering.py`, replay renderer-context exports | Provides project metadata, repo topology, world model, visual continuity, and history. |
| External renderer command | Proven by tests and fixtures | `tests/test_rendering.py`, `tests/test_server_and_cli.py`, examples renderers | This is the executable renderer-agent process boundary for 1.0. |
| Prompt-command model adapter | Proven by tests | `tests/test_model_renderer.py`, CLI replay model-command tests | Provider SDK integration remains deferred. |
| Render-plan validation | Proven by tests | `tests/test_rendering.py`, model/external renderer tests | Unsafe plans fail open into deterministic fallback and diagnostics. |
| Visual primitive/effect catalog | Proven by tests and screenshots | `tests/test_catalog.py`, browser integration, replay fixture screenshots | Catalog is stable enough for renderer prompts and alternate backends. |
| Browser display primary view | Proven by tests | `tests/test_browser_integration.py` | UX polish is not final, but primary/debug split is present. |
| Debug drawer close/reopen | Proven by browser tests | `tests/test_browser_integration.py` | Adequate for 1.0. |
| Display endpoints | Proven by tests | `tests/test_server_and_cli.py`, backend-contract tests | `/health`, `/scene`, `/catalog`, `/backend-contract`, SSE, and input endpoints are covered. |
| Non-web backend contract | Proven by tests/docs | `tests/test_server_and_cli.py`, `docs/integration/browser.md` | Actual alternate backend implementation is deferred. |
| Replay agent-side events | Proven by tests and fixtures | `tests/test_replay.py`, `examples/replays` | Acceptance checks final scenes and screenshots. |
| Replay renderer-side plans/mutations | Proven by tests and fixtures | `examples/replays/renderer-plan.json`, `tests/test_replay.py` | Useful for primitive/effect regression. |
| Watch replay | Proven by tests/docs | `tests/test_server_and_cli.py`, `docs/replay.md` | Manual visual review path exists. |
| Event-log conversion and splitting | Proven by tests/docs | `tests/test_replay.py`, `tests/test_server_and_cli.py`, `docs/replay.md` | Redaction metadata is part of promoted fixture flow. |
| Dogfood capture trajectories | Proven by tests/docs | `tests/test_server_and_cli.py`, `examples/prompts`, `docs/replay.md` | Built-in `tiny-project` and `repo-map` recipes exist. |
| Checked-in visual fixtures | Proven by acceptance | `examples/replays`, `examples/gibson1-replays`, `examples/dogfood-replays`, committed baselines | Browser screenshots are nonblank and baseline-checked. |
| Secret/runtime hygiene | Proven by tests and acceptance | `tests/test_project_config.py`, `scripts/acceptance.sh` | Final release should still inspect `git status` before tag. |
| Documentation | Mostly proven by inspection | `README.md`, `docs/architecture.md`, `docs/renderer-agent.md`, `docs/replay.md`, `docs/integration/browser.md` | A final reader pass is still worthwhile. |

## Remaining 1.0 Release Work

1. Run one short real dogfood session with harn/Codex, not just the `--harn-bin true` smoke. The purpose is to catch provider/auth/harn packaging issues that unit tests and smoke mode intentionally avoid.
2. Rerun `bash scripts/acceptance.sh` after any release-candidate edits.
3. Decide release metadata:
   - package version for 1.0;
   - changelog or release notes;
   - tag name and whether to push the tag.
4. Do a final docs pass for first-time users:
   - normal dogfood path;
   - renderer implementation path;
   - primitive/backend implementation path;
   - replay review workflow.
5. Confirm `git status --short --branch` is clean and only intended tracked files are in the release commit.

## Deferred After 1.0

These are valuable, but they should not block the 1.0 substrate release:

- Provider-SDK-backed renderer agent.
- Semantic code graph and test-to-code mapping.
- Richer agent attention/intent signals from harn.
- Stale/reconciled world-model facts.
- More coherent default visual direction beyond current `gibson1`.
- Lower-level binding vocabulary for world-model-backed spatial primitives.
- Alternate non-web display backend implementation.
